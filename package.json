{
  "name": "typescript-playground",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "tsc src/optional_payload.ts && node src/optional_payload.js",
    "start": "tsc && node build/src/index",
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "gts": "^3.1.0",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "@type-challenges/utils": "^0.1.1",
    "typescript": "^5.6.3"
  }
}
